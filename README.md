# Core Intermediate Runtime (CIR)

- core contains the runtime 
- frontend contains the frontend for interacting with the runtime via CLI


CIR is register-based 
256 registers each 8 bytes
uses computed gotos

